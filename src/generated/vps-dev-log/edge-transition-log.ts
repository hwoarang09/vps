// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

/**
 * Edge transition log
 */
export class EdgeTransitionLog {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):EdgeTransitionLog {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsEdgeTransitionLog(bb:flatbuffers.ByteBuffer, obj?:EdgeTransitionLog):EdgeTransitionLog {
  return (obj || new EdgeTransitionLog()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsEdgeTransitionLog(bb:flatbuffers.ByteBuffer, obj?:EdgeTransitionLog):EdgeTransitionLog {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new EdgeTransitionLog()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

vehId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

fromEdge():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

toEdge():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

nextEdges(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

nextEdgesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

nextEdgesArray():Uint32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? new Uint32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

pathBufLen():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

static startEdgeTransitionLog(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addVehId(builder:flatbuffers.Builder, vehId:number) {
  builder.addFieldInt32(0, vehId, 0);
}

static addFromEdge(builder:flatbuffers.Builder, fromEdge:number) {
  builder.addFieldInt32(1, fromEdge, 0);
}

static addToEdge(builder:flatbuffers.Builder, toEdge:number) {
  builder.addFieldInt32(2, toEdge, 0);
}

static addNextEdges(builder:flatbuffers.Builder, nextEdgesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, nextEdgesOffset, 0);
}

static createNextEdgesVector(builder:flatbuffers.Builder, data:number[]|Uint32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createNextEdgesVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createNextEdgesVector(builder:flatbuffers.Builder, data:number[]|Uint32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt32(data[i]!);
  }
  return builder.endVector();
}

static startNextEdgesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addPathBufLen(builder:flatbuffers.Builder, pathBufLen:number) {
  builder.addFieldInt32(4, pathBufLen, 0);
}

static endEdgeTransitionLog(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createEdgeTransitionLog(builder:flatbuffers.Builder, vehId:number, fromEdge:number, toEdge:number, nextEdgesOffset:flatbuffers.Offset, pathBufLen:number):flatbuffers.Offset {
  EdgeTransitionLog.startEdgeTransitionLog(builder);
  EdgeTransitionLog.addVehId(builder, vehId);
  EdgeTransitionLog.addFromEdge(builder, fromEdge);
  EdgeTransitionLog.addToEdge(builder, toEdge);
  EdgeTransitionLog.addNextEdges(builder, nextEdgesOffset);
  EdgeTransitionLog.addPathBufLen(builder, pathBufLen);
  return EdgeTransitionLog.endEdgeTransitionLog(builder);
}
}
